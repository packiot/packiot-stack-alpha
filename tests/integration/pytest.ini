[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --strict-markers
    --tb=short
markers =
    plc: PLC pipeline tests (PubSub → oeecloud → DB)
    operator: Operator HTTP pipeline tests (edge-api → user_logs)
