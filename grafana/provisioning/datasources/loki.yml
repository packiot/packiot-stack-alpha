apiVersion: 1

datasources:
  - name: Loki
    type: loki
    uid: packiot-loki
    url: http://loki:3100
    access: proxy
    isDefault: false
    editable: false
    jsonData:
      maxLines: 1000
