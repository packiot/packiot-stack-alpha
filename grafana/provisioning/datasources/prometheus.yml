apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: packiot-prometheus
    url: http://prometheus:9090
    access: proxy
    isDefault: false
    editable: false
    jsonData:
      timeInterval: 15s
      httpMethod: POST
