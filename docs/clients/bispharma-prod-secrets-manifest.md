# Bispharma prod — secret manifest (paste-only) + mTLS cert

**Status:** Fill-in template · **Date:** 2026-07-27 · **Task:** #37 ·
**Companion:** [`bispharma-prod-recut-runbook.md`](bispharma-prod-recut-runbook.md) §2

> Fill the `<PASTE …>` values in AWS Secrets Manager under `packiot/production/*`.
> Nothing here is a real secret — it is the shape the compose services expect. Each
> secret is a JSON blob unless noted. Region us-east-1.

## 1. Secrets — NOTHING TO PASTE (terraform owns them all)

**CORRECTION (2026-07-27, verified live):** every `packiot/production/*` secret is a
**terraform-generated `random_password`** and **already exists** in Secrets Manager
(us-east-1). The user provides **zero** secret values. Confirmed present:

| Secret ID | Generated by | Contains |
|---|---|---|
| `packiot/production/db` | `random_password.db` | port, name, user, password (no host — see below) |
| `packiot/production/hasura` | `random_password.hasura_admin/jwt` | admin_secret, jwt_secret |
| `packiot/production/app` | `random_password.{edge_api_key,rabbitmq,grafana_admin}` | edge_api_key, **rabbitmq_user/password**, grafana_admin_pass |
| `packiot/production/rabbitmq-oeecloud-creds` | (present) | oeecloud AMQP creds |
| `packiot/production/nginx-auth` | `random_password.nginx_auth` | username, password |
| `packiot/production/authentik` | 4× `random_password` | db_password, secret_key, bootstrap_password/token |
| `packiot/production/github-runner`, `packiot/production/ec2-rescue` | tf | runner token, rescue root pw |

> The earlier "generate + paste" instructions were WRONG — superseded. The DB password
> I generated in-session is **moot** (terraform's `random_password.db` is authoritative,
> `lifecycle.ignore_changes = [secret_string]` protects it).

**DB host wiring (the one real DB task, not a secret):** `packiot/production/db` deliberately
carries **no `host`** — `app_init.sh` sets `POSTGRES_HOST=${db_private_ip}` from a terraform
var. With #626 applied, the dedicated DB is at **`10.20.10.89`**; the compose/app_init must be
re-pointed from the old local container to that instance (the **W1 compose-parity rewire** —
part of deploy, not a paste).

## 2. mTLS cert (item #2) — CSR is ready

Generated an EC P-256 client key + CSR, `CN=bispharma` (ADR-0042 per-tenant convention).
Artifacts are in the job scratch dir (NOT committed — private key never enters git):
`$CLAUDE_JOB_DIR/tmp/bispharma-mtls/{bispharma-tee.key, bispharma-tee.csr}`.

**Your one action:** approve signing the CSR against the prod ingest CA (the front-door
`nginx` :8883 mTLS trust root, ADR-0042). Then the signed cert + the tenant key go to the
factory tee as its client identity. Store the signed cert ref (not the key) if the compose
uses a secret ref; the **key stays on the tee**.

> ⚠️ Security note: ideally the tee's private key is generated **on the tee device** and
> only the CSR travels. This key was generated here for onboarding speed — either transfer
> it over a secure channel, or (cleaner) regenerate on-device and re-issue. Your call; flag
> if you want the on-device path and I'll adjust the runbook.

## 3. Still need a literal answer from you

- **Factory public IP** (`/32`) — for the ingest SG allow-rule (item #1).
- **Bisnago users** — split the 13 `bisnago.ind.br` users to the bisnago enterprise? (item #4, y/n).

Everything else in the checklist I've now staged to paste-only or approve-only.
